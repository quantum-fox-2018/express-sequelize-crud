<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Teacher Page</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="main.css" />
    <script src="main.js"></script>
</head>
<body>
    <h1 style="text-align: center">Teacher page</h1>
    <a href="/teachers/addTeacher">add teacher</a>
    <table style="width:100%">
        <tr>
            <th>id</th>
            <th>First Name</th>
            <th>Last Name</th> 
            <th>Email</th>
            <th>Options</th>
        </tr>
        <% for(let index=0; index<teacherData.length; index++){ %>
            <tr>
                <th><%= teacherData[index].id%></th>
                <th><%= teacherData[index].first_name%></th>
                <th><%= teacherData[index].last_name%></th> 
                <th><%= teacherData[index].email%></th>
                <th><a href="/teachers/editTeacher?id=<%=teacherData[index].id%>">edit</a> | <a href="/teachers/deleteTeacher?id=<%=teacherData[index].id%>">delete</a></th>
            </tr>
        <%}%>
    </table>
</body>
</html>